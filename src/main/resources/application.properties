# PostgreSQL configuration
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.avkedwdwjynbpbdoekgq
spring.datasource.password=FSAD_Project_4
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Optional: format SQL output nicely
spring.jpa.properties.hibernate.format_sql=true

# Set the base path for all REST endpoints
spring.mvc.servlet.path=/api

# Enable detailed error messages for debugging
server.error.include-message=always
server.error.include-stacktrace=always

# HikariCP settings for connection pooling
spring.datasource.hikari.connection-timeout=15000
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.maximum-pool-size=5


